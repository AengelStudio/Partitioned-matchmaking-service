# Template only — do not commit real secrets.
# Create the real Secret out-of-band instead, e.g.:
#   kubectl create secret generic pms-secrets --namespace pms \
#     --from-literal=DATABASE_URL='postgresql://pms:REPLACE_ME@postgres:5432/pms' \
#     --from-literal=POSTGRES_PASSWORD='REPLACE_ME'
apiVersion: v1
kind: Secret
metadata:
  name: pms-secrets
  namespace: pms
type: Opaque
stringData:
  DATABASE_URL: "postgresql://pms:REPLACE_ME@postgres:5432/pms"
  POSTGRES_PASSWORD: "REPLACE_ME"
