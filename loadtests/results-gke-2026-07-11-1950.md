# GKE scale-out benchmark results — 5-node rerun

**Generated:** 2026-07-11 19:50 W. Europe Daylight Time  
**Cluster:** GKE zonal, `europe-west1-b`, project `se-proto`  
**Machine type:** e2-standard-4  
**Load script:** `loadtests/scale_out.js` (multi-tenant)  
**Run duration:** 180s  
**Access:** GCE ingress load balancer (`http://8.233.143.153`)  
**Conditions:** Same as 1842 suite — worker/api/dispatcher rollout restart before run, 5/5/4 replicas, ingress load test. Postgres `max_connections=200`. Cluster left at 5 nodes (`--skip-teardown`).

| Nodes | API / Worker / Dispatcher | Tickets created | Tickets rejected | matches_created/s | Tickets:matches ratio | p95 ticket-create latency |
|-------|---------------------------|-----------------|------------------|-------------------|----------------------|---------------------------|
| 5 | 5 / 5 / 4 | 4776 (26.53/s) | 53582 (297.68/s) | 22.29 (4013 matches / 180s) | 59.5% | 78.88ms |

## Raw worker metrics

- **5 node(s):** matches before=188, after=4201 (summed all 5 worker pods), delta=4013
- Note: `before` was read via `worker-metrics` service (single pod through LB). `after` is the sum across all worker pods.

## Admission control rejections

From API `/metrics` after the run, **summed across all 5 API pods** (`tickets_rejected_total`).

### 5 node(s)

| Reason | HTTP | Count | Share |
|--------|------|------:|------:|
| `rate_limit` | 429 | 97,419 | 99.9% |
| `tenant_quota` | 429 | 0 | 0.0% |
| `partition_overload` | 503 | 0 | 0.0% |
| `load_shedding` | 503 | 118 | 0.1% |

`rate_limit` rejections per pod were ~19.4k each (even spread across 11 tenants, ~1.7–1.8k per tenant per pod).

## Comparison to 2026-07-11 18:42 run

| Metric | 1842 @ 5 nodes | This rerun @ 5 nodes | 1842 @ 3 nodes |
|--------|----------------|----------------------|----------------|
| Total k6 responses (created + rejected) | ~24.5k | **~58.4k** | ~58.9k |
| Tickets created | 9429 | 4776 | 12836 |
| Tickets rejected (k6) | 15053 | **53582** | 46135 |
| matches/s | 5.28 | **22.29** | 8.53 |
| p95 latency | 60.7ms | 78.9ms | 70.4ms |
| API rate_limit in metrics | 0 (single-pod scrape) | 97,419 (all pods) | 15,342 (single-pod scrape) |

**Conclusion:** The 1842 5-node row was anomalous — traffic volume was ~42% of the 3-node run and API metrics showed zero rejections (likely a single-pod scrape of a freshly restarted pod). This rerun matches the **3-node traffic volume** (~58k responses) and shows heavy rate-limiting with functional match throughput (~22 matches/s vs 5.3 in the bad row).

## Notes

- k6 completed successfully; script failed afterward collecting post-run metrics (port-forward to `worker-metrics` died). Worker/API metrics above were collected manually by summing all pods.
- Worker deployment restarted before the run (same as 1842 suite).
- Postgres PVC was **not** reset (cluster stayed up; `--skip-deploy`).
